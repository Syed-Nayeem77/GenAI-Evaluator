# This file would be generated by your training script. Here's how to create it:

# train_model.py
from sklearn.ensemble import RandomForestClassifier
import joblib
import numpy as np
import os
from pathlib import Path

# Create sample training data
X_train = np.random.rand(100, 5)  # 100 samples, 5 features
y_train = np.random.randint(0, 2, 100)  # Binary classification

# Train model
model = RandomForestClassifier(n_estimators=100, random_state=42)
model.fit(X_train, y_train)

# Ensure directory exists
model_dir = Path("models/evaluator/v1")
model_dir.mkdir(parents=True, exist_ok=True)

# Save model
joblib.dump(model, model_dir / "model.pkl")

print(f"Model saved to {model_dir/'model.pkl'}")
